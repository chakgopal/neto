export default class UpdateModule {
    private data;
    constructor(data: any);
    update(data: any): this;
    exec({ debug, action, schema }: {
        debug?: boolean;
        action: string;
        schema: string;
    }): Promise<any>;
}
