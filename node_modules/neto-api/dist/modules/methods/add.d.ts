export default class AddModule {
    private data;
    constructor(data: any);
    add(data: any): this;
    exec({ debug, action, schema }: {
        debug?: boolean;
        action: string;
        schema: string;
    }): Promise<any>;
}
